# Extensions

Built-in extensions live under `extensions/builtin/*` so discovery, installation, and packaging can converge on one extension model. Add future marketplace or partner extensions beside this structure instead of coupling them into app shells.
