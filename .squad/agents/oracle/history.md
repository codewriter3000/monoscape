# Project Context

- **Owner:** Alex Micharski
- **Project:** monoscape
- **Stack:** SolidJS UI, microkernel architecture, extension discovery and installation, mobile and desktop first
- **Created:** 2026-04-21T06:58:17.132-04:00

## Learnings

- Monoscape is a word processor meant to compete with Google Docs and Microsoft Word.
- WCAG 2.2 ADA compliance is a core product requirement.
- The product is designed around college-student workflows.
- Extensions are part of the product strategy and should fit the microkernel architecture.
- The project is MIT licensed with no premium feature split.
- **[2026-04-21] Team Update from Neo:** Monorepo scaffold established with apps (desktop, mobile, web), packages (kernel, document-core, ui, extension-sdk), and built-in extensions (citations, review). Root developer foundation complete. Microkernel architecture boundary preserved via npm workspaces.
- **[2026-04-21] Base Editor Architecture (Oracle Review):** TextEditor uses SolidJS contentEditable with role="textbox"; FormattingToolbar implements arrow-key navigation with manual focus management and aria-pressed state. Semantic HTML structure (main > header/section/footer) is strong. Key accessibility gap: keyboard navigation does not provide clear exit from toolbar to editor (focus trap risk). Document defaults (12pt Liberation Serif, 1.5 line height) follow academic ergonomics. Decision inbox entries created and merged to .squad/decisions.md (Decisions #2, #3, #4).
- **[2026-04-21] Editor UX Patterns:** ContentEditable + execCommand pattern is stable and familiar to students; manual focus management in toolbar requires Tab/Arrow key coordination to avoid keyboard user entrapment. Button state signaling via aria-pressed is correct. Focus indicators use #4a90d9 outline (strong contrast). File paths: packages/ui/src/TextEditor.tsx, packages/ui/src/FormattingToolbar.tsx, packages/ui/src/index.tsx (MonoscapeShell).
- **[2026-04-21] Scribe Session:** Layout review audit findings merged to .squad/decisions.md. Key: toolbar focus trap (Concern 2, blocker), document affordance signal (Concern 1, design decision), default styling (Concern 3, product scope). All accessibility patterns meet WCAG 2.2 AA baseline. Trinity to implement focus escape fix.
- **[2026-04-21] BASE LAYOUT SESSION COMPLETE:** Oracle findings fully addressed. WCAG 2.2 AA baseline approved and documented (Decision #3: Formatting Toolbar Accessibility Specification). Keyboard focus escape blocker fixed by Trinity. Document affordance (Concern 1) and default styling (Concern 3) deferred to design/product phase per Oracle recommendations. Semantic HTML verified. Focus management with roving tabindex validated. Button state semantics (aria-pressed) confirmed. Typography baseline (12pt Liberation Serif) meets college-student readability requirements. Contrast verified WCAG AA (4.5:1 text, 3:1 UI). Desktop integration complete. Feature ready for merge. Orchestration log: .squad/orchestration-log/2026-04-21T12-14-19-oracle.md. Session log: .squad/log/2026-04-21T12-14-19-base-layout.md.
- **[2026-04-21] Font Controls Revision (Post-Switch Rejection):** Fixed three blockers in font-controls feature: (1) Added `uploadFonts` capability flag to hide upload button on web/mobile platforms (platform-conditional rendering pattern established); (2) Implemented custom font size input with 4–72pt validation and role="alert" error messages (validation + feedback pattern); (3) Added window.confirm() prompt before font removal (destructive action confirmation pattern). All changes surgical, preserving shipped feature. TypeScript clean. WCAG 2.2 AA verified. Key files: packages/ui/src/FormattingToolbar.tsx (validation logic, confirmation handler, conditional upload UI), packages/ui/src/TextEditor.tsx (uploadFonts prop), apps/desktop/src/App.tsx (uploadFonts: true flag). Decision: .squad/decisions/inbox/oracle-font-controls-revision.md. Patterns: platform capability flags for conditional rendering, custom input with inline validation and error feedback, confirmation prompts for irreversible actions.
- **[2026-04-21] FONT CONTROLS RE-REVIEW: APPROVED — Switch Verification:** Switch conducted re-review of Oracle's font-controls revision and approved for merge. All three blockers successfully fixed: (1) Platform-conditional upload visibility via uploadFonts capability flag + <Show when={...}> conditional, (2) Custom font size input (4–72pt) with blur/Enter validation, error messages with role=alert, aria-label, (3) Confirmation before font removal via window.confirm() with font name and irreversibility statement. Quality verified: TypeScript clean, tests pass, WCAG 2.2 AA compliance confirmed, keyboard navigation preserved, no console warnings. Patterns established for future use: platform capability flags, custom input with validation feedback, confirmation for destructive actions. Feature ready for merge. Decisions merged to .squad/decisions.md (Decision #19: Revision details, Decision #20: Re-review approval). Orchestration logs: .squad/orchestration-log/2026-04-21T11-22Z-oracle.md, .squad/orchestration-log/2026-04-21T11-25Z-switch-reapproval.md. Session log: .squad/log/2026-04-21T15-30Z-font-controls-revision.md.
- **[2026-04-21] FONT CONTROL REDESIGN: UX/ACCESSIBILITY REVIEW (Pre-Implementation):** Reviewed proposal to replace current two-select pattern (Filter dropdown + Font dropdown) with complex control combining font selection, filtering, search, and add-font actions (Docs/Word model). Findings: (1) **Concept Sound:** Combining functions in one control matches target audience mental model and competitors; no accessibility barriers identified if implemented correctly. (2) **Keyboard Navigation Required:** Must implement custom combobox-like behavior with arrow keys (open/navigate), Tab (flow), Escape (close), Enter (select), Home/End (jump), type-ahead (search). Cannot use native `<select>` element; requires manual focus management (roving tabindex pattern). (3) **Screen Reader Semantics:** Use `role="listbox"` on dropdown container, `role="button" aria-expanded="true|false" aria-haspopup="listbox"` on trigger, `role="option"` on font items. Announce search results count via `aria-live="polite"`. (4) **Selection-vs-Insertion Model:** Current code applies font to selection only; Docs/Word also apply pending font to insertion point (next text typed). Recommend adding `pendingFontFamily` state in TextEditor.tsx to track this and display in trigger button with aria-label ("Will apply to new text"). (5) **Implementation Complexity:** Doable with SolidJS; focus management skeleton provided in decision. No blockers, but requires careful testing (keyboard nav, screen reader, mobile touch model TBD). **Key Accessibility Patterns:** Manual focus management with roving tabindex, ARIA live regions for search feedback, clear exit paths (Escape always closes + returns focus to trigger). **WCAG 2.2 AA Compliance:** All checkpoints addressable (focus visible, keyboard access, aria attributes, status messages). Decision document: `.squad/decisions/inbox/oracle-font-control-review.md`. Guidance complete; ready for Trinity implementation.
