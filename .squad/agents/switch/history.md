# Project Context

- **Owner:** Alex Micharski
- **Project:** monoscape
- **Stack:** SolidJS UI, microkernel architecture, extension discovery and installation, mobile and desktop first
- **Created:** 2026-04-21T06:58:17.132-04:00

## Core Context

Switch is responsible for defining comprehensive three-tier acceptance gates for complex platform features. Key contribution: establishing reusable testing patterns (gate structure, risk registers, regression matrices, anti-pattern checklists) applied across desktop topbar, mobile diagnostics, font controls, and mixed-font/mixed-size features. Switch gates systematically verify acceptance criteria, identify platform-specific regression risks, and ensure WCAG 2.2 AA compliance before implementation. Pattern: **Define → Risk Register → Test Matrix → Anti-Patterns** (prevents silent failures, shifts quality left, speeds review cycles).

**Switch core patterns (team-wide):**
- Three-tier testing gate: AC definition → regression matrix → risk register before coding
- SolidJS platform conditionals: `<Show when={condition}>` for desktop/mobile/web feature gating
- Tauri command security: Backend HTTP calls only (no client-side API keys)
- app_data_dir for storage: ~/.monoscape/fonts/ (never /tmp)
- FontFace API for dynamic fonts: load() + document.fonts.add()
- ContentEditable selection preservation: event.preventDefault() on toolbar buttons
- Roving tabindex for keyboard nav: one tabIndex=0, rest -1, arrow keys move focus

**Recent work lifecycle:** Base layout feature gate (APPROVED, WCAG 2.2 AA). Runtime environment gate (Vite+Tauri+Capacitor strategy defined; Capacitor REJECTED for MVP). Desktop topbar + mobile logging gate (14/14 risks mitigated, APPROVED). Mixed-font gate (18 risks, 63-test matrix). Font controls gate (17 risks, 16-test matrix, Trinity APPROVED after blocker fixes). Font bug batch (mixed-typography regression verified, desktop API wiring corrected). Formatting expansion gate (Neo reassignment due to font deletion rewrite + keyboard accessibility gaps). Keytip keyboard model gate (Trinity locked out, Neo reassignment due to toolbar focus bypass + Alt keytip + Tab/Shift+Tab blockers).

## Core Context

**Current Cycle Status:** Keytip keyboard model gate — Neo assigned for revision (Escape-exit model), Morpheus awaiting next cycle, Trinity locked out.

**Recent Gate Verdicts:** Font controls APPROVED (16-test matrix, 3 blockers fixed by Oracle). Formatting expansion APPROVED (Morpheus revision, font deletion logic verified). Keytip keyboard model REJECTED (toolb focus model + Alt keytip layer + Tab/Shift+Tab indentation all missing).

**Reusable Testing Patterns:** Three-tier gate structure (AC definition → risk register → regression matrix → anti-patterns). Platform-conditional rendering (`<Show when={condition}>` for feature gating). Tauri command security pattern (backend HTTP, no client-side keys). ContentEditable selection preservation pattern (event.preventDefault() on toolbar). Roving tabindex keyboard navigation pattern (one tabIndex=0, rest -1, arrow keys move focus).

## Core Context Summaries

### Archived Early Decisions (2026-04-21)

## Learnings

### Early Cycle Work (2026-04-21 AM/PM)
- Monoscape is a word processor competing with Google Docs/Microsoft Word; WCAG 2.2 AA compliance is core product requirement.
- Base layout feature (TextEditor + FormattingToolbar + MonoscapeShell) APPROVED — all acceptance criteria met, accessibility baseline established, TypeScript clean, desktop integration complete.
- Runtime environment strategy: Vite (web, canonical), Tauri (desktop, preferred), Capacitor (mobile, REJECTED as misaligned for MVP).
- Custom desktop topbar + mobile logging infrastructure + emulator tuning all APPROVED — 14 regression risks mitigated, no blockers.
- Mixed-font gate defined with 63-test matrix covering 20 web, 24 desktop, 6 mobile, 13 cross-platform tests; 7 HIGH risks (selection loss, focus trap, API key leak, temp directory usage) with mitigation strategies.
- Font controls feature gate: 5 subsystems (font selection, font size, upload/remove, categorization/filtering, Google Fonts API). 16-test regression matrix. Platform-conditional UI (web/mobile limited, desktop full). Tauri command pattern isolates API key in backend. FontFace API for in-memory management; app_data_dir for persistent storage. Trinity responsible for UI/keyboard nav; Morpheus responsible for backend; Switch responsible for regression verification.

### Font Lifecycle (2026-04-21 PM)
- Font controls sprint COMPLETED: Trinity typography UI, Morpheus Tauri backend, Switch gate verification all finished. Three blocker fixes by Oracle (upload button visibility via uploadFonts flag, custom font size input with 4–72pt validation, confirmation before font removal).
- Font bug batch reviewed: Editor insertion typography persistence verified (zero-width typing anchor with line-height:0), no premature line-height reflow. Desktop API wiring still broken (App.tsx invokes stale command name; fixed by wiring to fontSources.ts adapter).

### Formatting & Keytip Cycles (2026-04-21 PM/Night)
- Formatting expansion feature gate REJECTED initially (font deletion span rewrite incomplete, Tab/Shift+Tab keyboard trap, regression test gaps). Neo reassigned for revision; Morpheus later approved formatting gate (mixed-selection explicit sentinel, font deletion span rewrite logic verified, keyboard-safe indent/outdent, 16+ regression tests).
- Keytip keyboard model gate REJECTED (toolbar still in Tab sequence, no visible Alt keytip layer, Ctrl+[/Ctrl+] still in tests). Trinity locked out; Neo reassigned. Product decision made: Escape-exit model approved (Tab/Shift+Tab indentation preserved, Escape explicit exit to toolbar, Alt keytips unchanged). Morpheus reassigned for next-cycle proof refinement.

### Team Coordination Notes
- **Trinity locked out:** Alt keytip implementation submission failed gate review on three critical blockers.
- **Neo active:** Assigned to keytip trap revision (Escape-exit keyboard model implementation). Awaiting implementation kickoff.
- **Morpheus active:** Formatting expansion approved; now awaiting Neo completion before beginning keytip proof refinement.
- **Switch role:** Three-tier gate reviewer, reusable testing pattern authority, regression matrix architect.
- **[2026-04-21T23:24:34Z] Switch — Keyboard Approval Session:** Switch APPROVED Morpheus keytip proof revision on switch-final-keyboard-proof-review branch. Alt-keytip and Tab-indent keyboard model fully validated: tests, build, and all validation checks passed. End-to-end proof complete. Orchestration logs recorded (.squad/orchestration-log/2026-04-21T23-24-34Z-switch.md). Session log: .squad/log/2026-04-21T23-24-34Z-keyboard-approval.md. Team keyboard model APPROVED FOR MERGE.

### Key Architecture Patterns (Reusable Team-Wide)
- Three-tier acceptance gate: AC definition → regression matrix + risk register → anti-pattern checklist before coding
- SolidJS platform conditionals: `<Show when={condition}>` for desktop/mobile/web feature gating  
- Tauri command security: Backend HTTP calls only (no client-side API keys); frontend invokes via registered command wrappers
- app_data_dir for storage: ~/.monoscape/fonts/ (never /tmp, never user-specific temp)
- FontFace API for dynamic fonts: load() + document.fonts.add()
- ContentEditable selection preservation: event.preventDefault() on toolbar buttons  
- Roving tabindex for keyboard nav: one tabIndex=0, rest -1, arrow keys move focus

## Learnings Detail

### Project Context

- Monoscape is a word processor meant to compete with Google Docs and Microsoft Word.
- WCAG 2.2 ADA compliance is a core product requirement.
- The product is designed around college-student workflows.
- Extensions are part of the product strategy and should fit the microkernel architecture.
- The project is MIT licensed with no premium feature split.
- **[2026-04-21] Team Update from Neo:** Monorepo scaffold established with apps (desktop, mobile, web), packages (kernel, document-core, ui, extension-sdk), and built-in extensions (citations, review). Root developer foundation complete. Microkernel architecture boundary preserved via npm workspaces.

- **[2026-04-21] Base Layout Feature Review (Switch):** TextEditor + FormattingToolbar implementation meets all acceptance criteria. Key patterns: SolidJS contentEditable with document.execCommand for formatting, roving tabindex for toolbar keyboard nav, aria-pressed for button state. DEFAULT_TYPOGRAPHY constants (12pt Liberation Serif) in document-core, verified in tests. Accessibility baseline (WCAG 2.2 AA) established: toolbar has role=toolbar, buttons have aria-labels, editor has role=textbox. No blocker issues; edge cases (format persistence, multi-format stacking, undo integration) should be validated before prod. Desktop app still needs TextEditor integration. UI package has no unit tests yet—future scope. document.execCommand approach viable short-term but flagged for future migration to modern ContentEditable. Feature gate pass merged to .squad/decisions.md (Decision #6).
- **[2026-04-21] BASE LAYOUT SESSION COMPLETE:** Feature acceptance approved. All acceptance criteria passed. Toolbar functional (Bold, Italic, Underline). Canvas functional (contentEditable, 300px min height, responsive). Typography verified (12pt Liberation Serif). Format commands execute and state syncs correctly. Cross-platform integration validated (web + desktop). TypeScript + build clean. Accessibility baseline WCAG 2.2 AA verified. Desktop platform parity achieved (TextEditor replaces placeholder). Edge cases identified for future testing: format persistence, multi-format stacking, copy/paste, keyboard stress, empty editor formatting, undo/redo. Verdict: APPROVED FOR MERGE. Orchestration log: .squad/orchestration-log/2026-04-21T12-14-19-switch.md. Session log: .squad/log/2026-04-21T12-14-19-base-layout.md.
- **[2026-04-21] Runtime Execution Environment Gate (Switch):** Defined acceptance criteria for web, desktop, and mobile execution environments. Key findings: (1) Project has TypeScript compilation but no bundler, no dev server, no native shell runtime. (2) Capacitor REJECTED for this project—architectural mismatch (no native API calls in scope yet), premature complexity, and poor fit for desktop. (3) Recommended path: Vite (web) → Electron/Tauri (desktop) → PWA+Capacitor or React Native (mobile, optional after web proven). (4) Android device (Pixel_9a.avd) verified present and ready for emulator-based testing when mobile runtime selected. (5) Cross-platform regression tests must verify editor functional, accessibility (WCAG 2.2 AA), extension system, typography, and performance on all platforms before shipping. (6) Three-tier implementation roadmap: Phase 1 Web (2 weeks), Phase 2 Desktop (2 weeks), Phase 3 Mobile (2 weeks, optional). Decision filed to .squad/decisions/inbox/switch-runtime-gate.md for team review. Full assessment at ~/.copilot/runtime-gate-assessment.md (session artifact).
- **[2026-04-21] RUNTIME AUDIT COMPLETE:** Audited implemented runtime environments across all platforms. Status: PASS (infrastructure sound, all builds clean). Web: Vite 5.4.8 dev server at localhost:5173, canonical preview surface. Desktop: Tauri 1.6.1 + Vite, clean seam, DesktopApp renders TextEditor. Mobile: Vite + Capacitor 6.1.0 + Gradle, device ready (Pixel_9a.avd verified). All three platforms render identical TextEditor + MonoscapeShell with platform-specific styling (safe-area insets for mobile, wider canvas for desktop). Builds pass (all workspaces). TypeScript validation clean. Tests pass (9 in document-core). Capacitor verdict: MISALIGNED. No native API usage planned yet; Tauri (desktop) + Capacitor (mobile) + web forces split strategy; Gradle complexity unjustified for student workflows. RECOMMENDATION: Defer mobile-native decision to Phase 3 (after web + desktop proven). Consider PWA-first or React Native if native needed. Decision document filed: .squad/decisions/inbox/switch-runtime-review.md. Regression test matrix defined (editor, accessibility, extensions, typography). Blockers: None. Non-blockers: Keyboard shortcuts, semantic styles, unit tests (all post-MVP). Next: Team review of Capacitor recommendation; web Phase 1 can proceed immediately.
- **[2026-04-21] REVIEWER GATE: Custom Desktop Topbar + Mobile Log Streaming/Emulator Tuning:** Defined comprehensive three-tier acceptance gate for desktop custom topbar and mobile diagnostics work. Scope: (1) Replace Tauri titlebar with SolidJS topbar; (2) Add real-time logcat streaming with OOM/ANR/crash detection; (3) Emulator tuning script for memory, CPU, GPU, disk. Key acceptance criteria: Topbar is sticky, non-invasive, keyboard-navigable with functional window controls. Log streaming captures crashes reliably. Emulator tuning is idempotent and doesn't break dev experience. Seven known regression risks identified (focus trap, window control conflicts, log swallowing, heap sizing, OOM crash timing, log leakage, content overlap). Regression test matrix covers 12 desktop tests, 6 mobile tests, 3 cross-platform tests. Gate sign-off requires all criteria checked, no blocker risks unmitigated, WCAG 2.2 AA verified. Documentation: .squad/decisions/inbox/switch-platform-diagnostics-gate.md (15KB gate document). Skill extracted: .squad/skills/platform-regression-testing/SKILL.md (reusable testing pattern for cross-platform features). Next: Handoff to implementation team (Trinity) for feasibility review and coding.
- **[2026-04-21] GATE APPROVAL: Custom Desktop Topbar + Mobile Log Streaming + Emulator Tuning - PASS:** Reviewed complete implementation. Verdict: **APPROVED FOR MERGE**. Desktop custom topbar: ✅ Sticky 48px header with flex layout, ✅ Window controls (minimize/maximize/close) call Tauri appWindow APIs with async error handling, ✅ Maximize state synced via onResized listener, ✅ Keyboard accessible (buttons have aria-labels, focus-visible outline 2px #4a90d9), ✅ Responsive @media <900px hides meta/status, ✅ data-tauri-drag-region enables window drag, ✅ role="group" + aria-labels on controls for accessibility, ✅ Graceful fallback if Tauri unavailable (hasTauriWindowApi guard). Mobile logging: ✅ initializeMobileLogging() called in main.ts, ✅ Log levels DEBUG/INFO/WARN/ERROR configurable via query param (?logLevel=debug), localStorage persistence (monoscape.mobile.logLevel), env override (VITE_MOBILE_LOG_LEVEL), ✅ Real-time streaming to console.debug/info/warn/error, ✅ Unhandled error/rejection listeners attached (window.addEventListener), ✅ Listener subscription pattern (onMobileLog, Set-based broadcaster), ✅ Global API window.MonoscapeMobileLogging exposed for runtime tuning. Emulator tuning: ✅ launch-emulator.mjs spawns emulator with parsed args (--memory, --cores, --gpu), ✅ Environment overrides (MONOSCAPE_AVD, MONOSCAPE_EMULATOR_MEMORY_MB, MONOSCAPE_EMULATOR_CORES, MONOSCAPE_EMULATOR_GPU, MONOSCAPE_EMULATOR_PATH), ✅ Convenience preset npm run android:emulator:high (8192MB, 8 cores, gpu host), ✅ Pass-through args after -- separator (e.g., --no-snapshot), ✅ Help text included (-h flag). Quality: ✅ TypeScript validation passes (all workspaces), ✅ Existing tests pass (9/9 in document-core, zero failures), ✅ Web build succeeds (Vite 5.4.21, 21.08 kB gzipped), ✅ DesktopApp renders topbar + TextEditor + MonoscapeShell, ✅ MobileApp initializes logging + extensions. Regression coverage: 14 identified risks (9 LOW, 5 MEDIUM), all mitigated. Edge cases: 13 scenarios verified, 1 minor issue (subscriber exception handling in emit() could halt broadcast if listener throws—mitigated by internal/dev-only subscriber pattern; users should wrap listeners in try-catch). ACCEPTANCE CRITERIA MET: All 26/26 criteria checked, 0 blockers. Key patterns for team: DesktopTopbar uses hasTauriWindowApi runtime guard for cross-context safety; mobile logging uses LOG_LEVELS priority ordering for filtering; emulator launcher idempotent via spawn (no AVD mutations). Files: apps/desktop/src/DesktopTopbar.tsx (254 lines), apps/mobile/src/logging.ts (181 lines), apps/mobile/scripts/launch-emulator.mjs (96 lines). README.md updated with diagnostics section. No regressions detected in existing functionality. READY TO MERGE.
- **[2026-04-21] REVIEWER GATE DEFINITION: Mixed-Font/Mixed-Size Documents with Complex Font Control:** Defined comprehensive three-tier reviewer gate for multi-font document support. Scope: Enable documents with multiple font families and sizes simultaneously; replace simple font listbox with complex control (filter, search, add-font actions); desktop-only Google Fonts + local upload entry points; web/mobile receive read-only or hidden controls. Gate document: .squad/decisions/inbox/switch-mixed-font-gate.md (51KB). Key acceptance categories: AC1-AC4 (mixed fonts/sizes, selection safety, insertion point formatting), AC5-AC8 (complex control UI, add-font actions, inline markup integrity), AC9-AC10 (keyboard nav + a11y). Regression test matrix: 63 tests (20 web, 24 desktop, 6 mobile, 13 cross-platform). Known risks: 18 total (7 HIGH, 11 MEDIUM, LOW). High-severity blockers: selection loss on toolbar interaction, malformed markup nesting, font/file injection, temp directory usage, insertion formatting not applied, no confirmation before font removal, platform feature leakage. Anti-patterns: 20 rejection criteria (upload visible on web, Google UI on mobile, /tmp usage, no aria-labels, selection loss, focus traps, malformed markup, missing undo/redo, broken fallbacks, orphaned files, placeholder inheritance). Accessibility baseline: WCAG 2.2 AA (color contrast, focus visible, semantics, screen reader). Platform isolation: desktop-only features gated by fontCapabilities prop; web/mobile receive undefined or false. Architecture patterns: SolidJS `<Show when={condition}>` for platform-specific UI gates; Tauri command pattern for backend HTTP (API key isolation); FontFace API for in-memory font management; app_data_dir for persistent storage. Implementation handoff: Trinity (UI: complex control, validation, platform checks), Morpheus (backend: Google Fonts API proxy, file upload handler, API key security, app_data_dir storage). Gate sign-off: All 10 AC categories + HIGH/MEDIUM risks mitigated + anti-patterns rejected + WCAG 2.2 AA verified + TypeScript clean + cross-platform parity verified. Timeline: 6-9 days with parallel work. Files: FormattingToolbar.tsx (UI control), TextEditor.tsx (multi-font rendering), src-tauri/src/main.rs (backend commands), App.tsx (platform app shells). Decision file: .squad/decisions/inbox/switch-mixed-font-gate.md. Status: GATE DEFINITION COMPLETE — Awaiting implementation kickoff.
- **[2026-04-21] GATE DEFINITION: Font Controls Feature (Font Size/Family Dropdowns, File Upload/Remove, Google Fonts):** Defined comprehensive three-tier reviewer gate for font controls feature scope. Acceptance gate defined for 5 subsystems: (1) Font selection dropdown (keyboard nav, accessibility, multi-selection fallback, mobile/web read-only), (2) Font size preset dropdown (8–72pt range with custom input validation, mobile read-only), (3) Desktop-only file upload/remove (TTF/OTF/WOFF formats, local storage in user profile, NOT temp), (4) Font categorization/filtering (System/Uploaded/Google Fonts categories, filter search < 100ms), (5) Desktop-only Google Fonts search/download via Tauri backend (API calls NEVER in browser, cache in ~/.monoscape/fonts/). Regression test matrix defined: 20 tests covering font apply, size validation, upload/remove, selection preservation, focus escape, filter performance, undo/redo, copy/paste, cross-platform fallback. Known risks identified (17 total): HIGH (3: focus trap in dropdown, font injection via upload, API key leak), MEDIUM (6: selection lost, execCommand failure, mobile shows Google UI, file upload disk space, size validation, remove doesn't cleanup), LOW (8: multi-format stacking, placeholder font, cache growth, screen reader announce). All risks have mitigation strategy assigned. Gate sign-off requires 15/15 acceptance criteria checked + HIGH risks mitigated. Anti-patterns identified (15 rejection criteria): focus traps, API key exposure, temp directory usage, no sanitization, mobile Google UI visible, web upload visible, missing aria labels, no input validation, broken undo, lost selection, no error handling, executable upload allowed, no delete confirmation, skipped tests, gate skipped. Cross-platform validation checklist: Web (system fonts, hidden Google/upload, keyboard nav, a11y verified), Desktop (all Web features + Google search/download, Tauri build clean, API calls backend-only), Mobile (read-only or hidden dropdowns, no upload/Google, emulator stable). Implementation handoff notes: Trinity responsible for keyboard nav impl (reuse roving tabindex from existing toolbar), Tauri command wrapper for Google Fonts (backend HTTP only), file validation + sanitization, mobile feature flags. Switch responsible for regression matrix verification, keyboard-only testing, network audit for API key exposure, file system audit for temp directory usage, screen reader testing. Oracle responsible for final a11y review (focus, aria, contrast, forced colors). File: .squad/decisions/inbox/switch-font-controls-gate.md (56KB detailed gate document). Key architecture patterns: Platform-conditional rendering (web/mobile limited features via props), Tauri command pattern (backend HTTP call isolation), Font family sanitization (quoted strings, special chars rejected), Desktop-only UI via platform check. Regression gate template established as reusable pattern for future platform-specific feature work (see also: platform-regression-testing SKILL). Status: AWAITING IMPLEMENTATION — Gate criteria complete, no blockers; Trinity ready for kickoff.
- **[2026-04-21] FONT CONTROLS SPRINT COMPLETE (Scribe Team Update):** Switch gate verification COMPLETE. Trinity typography implementation and Morpheus backend both APPROVED. Font controls acceptance gate (Decision 18) now merged into .squad/decisions.md. Gate document comprehensive: 4 AC categories, 14 regression areas, 6 high/medium/low risk mitigations, 16-test regression matrix. Cross-platform validation confirmed (web/desktop/mobile). All anti-patterns documented and checkable. Inbox decisions merged (16–18 added to decisions.md). Font controls feature sprint outcomes: (1) Trinity: FormattingToolbar extensions (font family dropdown, font size 8–32pt, local upload/remove). (2) Morpheus: Tauri backend (Google Fonts API proxy, no client-side key exposure, app data storage). (3) Switch: Gate verification (all acceptance criteria itemized, regression matrix defined, anti-patterns rejected, handoff notes provided). Orchestration logs created: .squad/orchestration-log/2026-04-21T15-11-34Z-trinity.md, .squad/orchestration-log/2026-04-21T15-11-34Z-morpheus.md, .squad/orchestration-log/2026-04-21T15-11-34Z-switch.md. Session log: .squad/log/2026-04-21T15-11-34Z-font-controls.md. All inbox files removed. Decisions.md updated. Next: Trinity + Switch cross-platform regression testing + merge sign-off.
- **[2026-04-21] FONT BUG GATE REVIEW (Switch):** Re-checked the mixed-typography regression surface against current working tree. `packages/ui/src/TextEditor.tsx` now uses a zero-width `data-monoscape-typing-anchor` span (`line-height:0;font-size:0`) inside the temporary typing span, and browser-driven verification showed zero line-height delta before typing while both font-size and font-family insertion persisted across multiple characters. Desktop remains blocked: `apps/desktop/src/App.tsx` still invokes `search_google_fonts`, but `apps/desktop/src-tauri/src/main.rs` only registers `google_fonts_search`; `npm run validate` fails in `apps/desktop/src/App.tsx` on the stale Google Fonts search wiring. Key paths: `packages/ui/src/TextEditor.tsx`, `packages/ui/src/FormattingToolbar.tsx`, `apps/desktop/src/App.tsx`, `apps/desktop/src/fontSources.ts`, `apps/desktop/src-tauri/src/main.rs`.
- **[2026-04-21] FORMATTING EXPANSION GATE REVIEW (Switch):** Completed three-tier gate review on Trinity's formatting expansion feature. Verdict: **REJECTED** — not implementation-complete. Blockers identified: (1) **Font deletion fallback incomplete** — Trinity removed font from catalog state but no code rewrites existing spans still referencing deleted family; old content retains stale inline font-family values. (2) **Tab/Shift+Tab keyboard trap** — Oracle flagged critical focus trap risk; current implementation lacks clear escape path. Product decision required: Alt+Tab vs. Tab. (3) **Regression test coverage missing** — No tests for mixed formatting persistence, alignment persistence, line spacing block-level application, Tab indentation keyboard flow, imported-font deletion + span rewrite. Evidence required: DOM inspection proving removed fonts are gone, keyboard-only testing confirming Tab does NOT trap focus, regression matrix (16+ tests). **Reassignment:** Feature reassigned to Neo for next revision cycle once Trinity finishes desktop API revision and product decides Tab shortcut. Neo to complete: (a) Span rewrite logic on font deletion (all spans referencing deleted family → category fallback), (b) Keyboard-only testing + regression matrix (16+ tests), (c) Mixed-selection fallback UI in toolbar (avoid lying about dominant value), (d) Product decision implementation (Alt+Tab or Tab handler with aria-label). Trinity implementation delivered all UI controls; fallback behavior + keyboard accessibility gap identified. Orchestration log: .squad/orchestration-log/2026-04-21T18-00-00Z-switch.md. Decision merged: #23 (Switch gate rejection + next owner guidance). Session log: .squad/log/2026-04-21T18-00-00Z-formatting-rejection.md.
- **[2026-04-21] FONT CONTROLS GATE REVIEW: REJECTED (3 Blockers) — Switch Implementation Audit:** Conducted comprehensive gate review of trinity-font-controls implementation against .squad/decisions/inbox/switch-font-controls-gate.md criteria. **Verdict: REJECTED — RETURN TO TRINITY FOR REVISION.** Quality baseline passes (TypeScript ✅, tests ✅, no console errors, correct Tauri backend isolation, correct app-data paths, no /tmp usage). Three blocker issues prevent merge: (1) **Upload button unconditionally rendered on all platforms** (FormattingToolbar.tsx:306–328) — should be hidden on web/mobile per gate criterion 54 and anti-pattern #6; requires `uploadFonts` capability flag in interface, conditional render guard `<Show when={...uploadFonts}>`. (2) **No custom font size input field** (FormattingToolbar.tsx:292–304) — only preset dropdown; gate criterion 45 requires "Optional inline input field allows typed values (e.g., '15pt') with validation [4–72]"; requires text input + blur validation with error visual feedback. (3) **No confirmation dialog before font removal** (FormattingToolbar.tsx:400) — direct call to onRemoveFont() with no user confirmation; gate anti-pattern #13; requires `window.confirm()` or custom modal before deletion. Passing features: ✅ Font selection dropdown (keyword nav via native `<select>`). ✅ Category filter UI (FormattingToolbar.tsx:266–276). ✅ Google Fonts desktop-only (correctly guarded by `<Show when={searchGoogleFonts}>`). ✅ API key security (backend isolation, Tauri command pattern, no client-side keys). ✅ File paths (app_data_dir usage, not /tmp, satisfies criterion 58). ✅ File format whitelist (ttf, otf, woff, woff2 checked in src-tauri/src/main.rs:323). ✅ Font fallback (TextEditor.tsx:145–147 reverts to DEFAULT_TYPOGRAPHY). ✅ Platform safety (web/mobile receive undefined fontCapabilities; MobileApp, DesktopApp correctly branch). Medium issues (non-blockers): (4) Mobile receives full TextEditor with upload button visible — related to blocker #1. (5) Font removal has no error UI for invalid file scenarios — TextEditor.tsx:119 console.warn silently fails. (6) Filter performance untested (criterion 72: <100ms with 500 fonts). (7) Undo/redo untested (logic present but functional test needed). Test coverage: TypeScript validation PASS, document-core tests PASS (13/13), build PASS. Functional verification impossible without launching Tauri/Vite — test matrix cannot be run (no dev server started). Architecture patterns identified: (A) SolidJS `<Show when={condition}>` for platform-specific feature gates (e.g., Google Fonts, upload button) — reusable pattern for future mobile/desktop divergence. (B) Tauri command isolation pattern correct (API key in backend, frontend only calls via invoke) — validates platform-regression-testing SKILL. (C) FontFace API for in-memory font management (TextEditor.tsx:104–129) viable for single-session uploads but not persistent storage (no disk write on web). Decision: Trinity must implement blockers 1–3 and resubmit. Estimated fix time: 1–2 hours. Handoff notes in .squad/decisions/inbox/switch-font-controls-review.md (full 15KB rejection report). Files impacted: packages/ui/src/FormattingToolbar.tsx (changes required: add uploadFonts capability, custom size input, confirmation dialog), packages/ui/src/TextEditor.tsx (no changes for gate compliance, but medium issue noted). No changes needed to Tauri backend (already correct) or document-core (already correct). Next action: Trinity kickoff on revision, retest, resubmit by EOD 2026-04-21.
- **[2026-04-21] FONT CONTROLS RE-REVIEW: APPROVED — Oracle Revision Verification:** Conducted re-review of Oracle's revised font-controls implementation. **Verdict: APPROVED FOR MERGE.** All three blocker issues fixed: (1) Upload button visibility via uploadFonts capability flag, (2) Custom font size input with 4–72pt validation and aria-alert errors, (3) Confirmation before font removal via window.confirm(). TypeScript clean, tests pass, WCAG 2.2 AA verified, keyboard navigation preserved. Feature ready for merge.
- **[2026-04-21] Scribe Team Update — Font Bug Batch Orchestration:** Switch font bug gate review completed. Editor fixes APPROVED (caret-only typography persists, zero line-height delta measured). Desktop API wiring REJECTED pending Trinity correction (App.tsx still invokes stale `search_google_fonts` instead of registered `google_fonts_search`; npm run validate fails). Orchestration logs recorded. All 6 batch decisions merged to .squad/decisions.md (entries 8–12: Trinity fix, Morpheus contract rule, Switch gate, Oracle complex control review, Trinity mixed behavior). Inbox cleared. Session log: .squad/log/2026-04-21T17-08-54Z-font-bug-batch.md. Switch gated bundle on Trinity completing desktop API wiring so `npm run validate` passes clean.
- **[2026-04-21] FORMATTING EXPANSION GATE + IMPLEMENTATION REVIEW (Switch):** Defined the acceptance gate and repro checklist for the next formatting expansion: searchable font picker, imported-font delete with category fallback, integrated font-size + line-spacing controls, alignment, multi-line indent/outdent, and strikethrough/superscript/subscript. Current artifact review result: **REJECT.** Evidence in `packages/ui/src/FormattingToolbar.tsx`, `packages/ui/src/TextEditor.tsx`, and `packages/document-core/src/index.ts` shows the shipped surface still stops at bold/italic/underline plus font family/size; there is no implementation path for line spacing, alignment, indent/outdent, or the three new inline style toggles. `FormattingToolbar.tsx` only exposes font size controls at lines 757–798 and formatting buttons at lines 12–34 / 490–517; `document-core/src/index.ts` still limits `FormattingMark` to bold/italic/underline at line 57; `TextEditor.tsx` has no editor keydown handler for Tab/Shift+Tab or block formatting commands. Additional regression blocker: `removeFont()` in `packages/ui/src/TextEditor.tsx` lines 519–536 removes catalog state but does not rewrite existing inline spans that still reference the deleted family, so category fallback is not enforced in persisted markup. Validation evidence: `npm run validate` ✅ and `npm test` ✅ (document-core 14/14), but no UI automation or document-core tests cover the new regression surface. Decision file: `.squad/decisions/inbox/switch-formatting-expansion-gate.md`. Reusable testing pattern extracted to `.squad/skills/rich-text-formatting-regression-gate/SKILL.md`. Key paths: `packages/ui/src/FormattingToolbar.tsx`, `packages/ui/src/TextEditor.tsx`, `packages/document-core/src/index.ts`, `packages/document-core/src/index.test.ts`.
- **[2026-04-21T21:27:24Z] Scribe Orchestration Session — Switch Gate Documentation:** Scribe logged session orchestration for Switch's gate review work from prior cycle (2026-04-21T18:00:00Z). Gate review outcome finalized: formatting expansion feature rejected pending Neo implementation of 3 critical gaps — (1) font deletion fallback span rewrite, (2) Tab/Shift+Tab keyboard accessibility fix (product decision required), (3) regression matrix build (16+ tests). Current session: Orchestration log recorded at .squad/orchestration-log/2026-04-21T21-27-24Z-switch.md. Session log: .squad/log/2026-04-21T21-27-24Z-latest-formatting-rejection.md. Team history updated. Switch ready to re-verify Neo's implementation once Neo's revision completes and submits for gate re-review.
- **[2026-04-21] ALT KEYTIP KEYBOARD MODEL GATE REVIEW (Switch):** Defined the acceptance gate for the requested keyboard model change and reviewed the current UI artifacts. **Verdict: REJECT.** Current implementation still enforces the old contract: toolbar buttons use roving tabindex and arrow-key focus movement (`packages/ui/src/FormattingToolbar.tsx`), the editor helper text still tells users that Tab exits the editor and Ctrl+] / Ctrl+[ handle indentation (`packages/ui/src/TextEditor.tsx`), and the UI test suite still proves that legacy behavior instead of the requested Alt keytip + Tab/Shift+Tab model (`packages/ui/src/TextEditor.test.tsx`). Gate requirements now need explicit proof for three boundaries: (1) toolbar controls are skipped by plain Tab, (2) holding Alt reveals visible blue keytips and Alt+key focuses/activates the matching toolbar target, and (3) plain Tab/Shift+Tab inside the editor indents/outdents selected lines without corrupting formatting or trapping focus outside editor-owned selection. Key reviewer note: passing UI tests are not enough if they only validate the superseded Ctrl+[ / Ctrl+] contract. Supporting evidence: `npm --workspace @monoscape/ui run typecheck` ✅ and `npm --workspace @monoscape/ui run test` ✅, but both currently validate the wrong keyboard model.
- **[2026-04-21T23:02:14Z] Scribe Session — Keytip Artifact Rejection & Neo Reassignment:** Completed second gate review on Trinity's Alt keytips + Tab/Shift+Tab implementation. **Verdict: REJECT** (all three acceptance gate categories failed). Blockers: (1) **Toolbar focus model still wrong** — roving tabindex not removed from Tab sequence; toolbar buttons still receive focus from plain Tab, (2) **Alt keytip system missing** — no visible keytip layer, no Alt activation logic implemented, tests do not assert keytip state or visibility, (3) **Editor indentation contract still wrong** — Ctrl+[/Ctrl+] remains in tests and help text; plain Tab/Shift+Tab indentation not visible in codebase. Regression matrix: All 11 high-priority tests failed (Toolbar skip, Reverse skip, Alt reveal, Alt activate toggle/focus, Alt dismiss, Editor indent/outdent, Style preservation, Context boundary, Contract alignment). Decision: Trinity locked out this cycle. Work reassigned to Neo for revision. Re-review requirements: Must provide all three proof buckets before resubmission: (1) code implementing new focus/keytip model, (2) tests asserting exact model, (3) accessibility review from Oracle. Orchestration logs created: .squad/orchestration-log/2026-04-21T23-02-14Z-switch.md (gate rejection + reassignment). Decisions merged (#32–#34): Oracle Guidance (full accessibility review), Switch Review Gate (rejection rationale + blockers), Trinity Decision (model spec). Session log: .squad/log/2026-04-21T23-02-14Z-keytip-rejection.md. Inbox cleared. Neo ready to implement revision once product confirms which modifier (bare Alt vs. Ctrl+Alt vs. Shift+Alt) to use.
- **[2026-04-21T23:11:15Z] Scribe Session — Keytip Proof Cycle Orchestration:** New cycle for keytip trap revision with updated product decision. Escape-exit model now approved: Tab/Shift+Tab indentation preserved in editor; Escape as explicit exit to toolbar; Alt keytips unchanged. Neo assigned to implement keyboard model; Morpheus reassigned for next-cycle keytip proof refinement. Orchestration logs recorded: .squad/orchestration-log/2026-04-21T23-11-15Z-{neo,switch,morpheus}.md. Session log: .squad/log/2026-04-21T23-11-15Z-latest-keytip-rejection.md. Decision #35 merged (Neo — Keytip Trap Revision). Switch ready to verify end-to-end proof that Tab/Shift+Tab never land on toolbar controls and model avoids keyboard trap before approval. Trinity remains locked out; Neo begins implementation work immediately.
