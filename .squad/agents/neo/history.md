# Project Context

- **Owner:** Alex Micharski
- **Project:** monoscape
- **Stack:** SolidJS UI, microkernel architecture, extension discovery and installation, mobile and desktop first
- **Created:** 2026-04-21T06:58:17.132-04:00

## Learnings

- Monoscape is a word processor meant to compete with Google Docs and Microsoft Word.
- WCAG 2.2 ADA compliance is a core product requirement.
- The product is designed around college-student workflows.
- Extensions are part of the product strategy and should fit the microkernel architecture.
- The project is MIT licensed with no premium feature split.
- **[2026-04-21] Scribe Session — Neo Reassignment:** Formatting expansion feature reassigned to Neo from Trinity due to gate rejection. Trinity's implementation delivered all UI controls (searchable font picker, alignment, strikethrough/superscript/subscript, Tab/Shift+Tab indentation); however, three critical gaps prevent merge: (1) Font deletion does not rewrite existing spans to fallback (stale font-family values linger), (2) Tab/Shift+Tab keyboard flow lacks clear escape path (Oracle flagged critical focus trap; product decision required: Alt+Tab vs. Tab), (3) Regression test coverage insufficient (no tests for mixed formatting persistence, alignment, line spacing, keyboard flows). **Neo assigned to complete:** (a) Span rewrite logic on font deletion (all spans referencing deleted family → category fallback), (b) Keyboard-only testing + regression matrix (16+ tests covering mixed formatting, alignment, copy/paste), (c) Mixed-selection fallback UI in toolbar (avoid lying about dominant value), (d) Product decision implementation (Alt+Tab or Tab handler with aria-label). Trinity remains on desktop font API revision. Key paths: `packages/ui/src/TextEditor.tsx` (font deletion fallback rewrite), `packages/ui/src/FormattingToolbar.tsx` (mixed-selection fallback UI), `packages/document-core/src/index.test.ts` (regression matrix). Orchestration logs: .squad/orchestration-log/2026-04-21T18-00-00Z-trinity.md (implementation complete), .squad/orchestration-log/2026-04-21T18-00-00Z-switch.md (gate rejection + reassignment). Decisions merged #21–#23. Session log: .squad/log/2026-04-21T18-00-00Z-formatting-rejection.md.
- Reserved `packages/kernel`, `packages/document-core`, `packages/ui`, and `packages/extension-sdk` as the first shared layers for bootstrapping, student workflows, shared SolidJS UI, and extension authoring.
- Added thin client shells at `apps/desktop`, `apps/mobile`, and `apps/web`; web is a preview surface while desktop/mobile remain runtime-agnostic until native container choices are validated.
- Created built-in extension packages at `extensions/builtin/citations` and `extensions/builtin/review` so extension discovery and installation can evolve around one package shape.
- Root developer foundation now lives in `package.json`, `tsconfig.base.json`, `README.md`, and `LICENSE`.
- **[2026-04-21] Scribe Session:** Layout review reconciliation complete. Trinity's implementation plan (centered canvas, desktop TextEditor integration) and Oracle's accessibility findings (focus escape blocker, styling defaults) all merged to .squad/decisions.md (Decisions #1–7). Desktop app TextEditor integration decision logged (Decision #7). Team ready for implementation.
- Cross-app drafting layout belongs in `packages/ui/src/index.tsx` via `MonoscapeShell`; app shells should pass content, not re-implement canvas framing.
- Shared editor chrome belongs in `packages/ui/src/TextEditor.tsx` and `packages/ui/src/FormattingToolbar.tsx`, with typography sourced from `packages/document-core/src/index.ts`.
- `apps/web/src/App.tsx` and `apps/desktop/src/App.tsx` now both consume the shared editor surface, keeping platform shells thin and the microkernel boundary intact.
- **[2026-04-21] BASE LAYOUT SESSION COMPLETE:** All agents (Trinity, Oracle, Switch) completed work on base layout feature. Trinity implemented shared TextEditor, FormattingToolbar, MonoscapeShell components. Oracle reviewed and approved accessibility baseline (WCAG 2.2 AA). Switch completed acceptance testing. All acceptance criteria met. Desktop app TextEditor integration complete. Keyboard focus escape blocker resolved. Build clean. Decisions #1–#7 merged to .squad/decisions.md. Trinity's implementation plan confirmed (Decision #5 revised). Feature ready for merge. Orchestration logs: .squad/orchestration-log/2026-04-21T12-14-19-{neo,trinity,oracle,switch}.md. Session log: .squad/log/2026-04-21T12-14-19-base-layout.md.
- Runtime direction: keep `apps/web` as the canonical browser-hosted shell, use Capacitor for `apps/mobile` so the shared SolidJS UI can run inside native Android/iOS wrappers, and avoid Capacitor for desktop because it weakens shell boundaries and adds unnecessary runtime bulk.
- Desktop runtime choice: prefer Tauri as the first native desktop container for `apps/desktop`, preserving the thin-shell model around the shared editor UI while keeping bundle size and host overhead lower than Electron-style packaging.
- User environment note: the existing Android emulator target is `C:\Users\alex.MICHARSKI\.android\avd\Pixel_9a.avd`; mobile runtime work should target Android first and wire the Capacitor Android project to that emulator during local execution.
- **[2026-04-21] Formatting blocker revision:** Imported-font removal must rewrite existing inline `font-family` styles in `packages/ui/src/TextEditor.tsx`, not just swap active toolbar state. Raw `Tab` in the shared `contentEditable` editor is not acceptable because it traps keyboard users; preserve line-indent behavior via toolbar buttons plus explicit modifier shortcuts, and expose the escape path in visible help text. Supporting font-family parsing now lives in `packages/document-core/src/index.ts` with regression coverage in `packages/document-core/src/index.test.ts`.
- **[2026-04-21T21:27:24Z] Scribe Orchestration Session — Neo Formatting Expansion Continuation:** Scribe logged session orchestration for Neo's formatting expansion feature work from prior cycle (2026-04-21T18:00:00Z). Prior reassignment scope remains active: Complete formatting expansion feature to address Switch gate blockers — (1) font deletion fallback span rewrite (TextEditor.tsx removeFont function must rewrite all spans referencing deleted family to category fallback), (2) Tab/Shift+Tab keyboard accessibility fix (product decision required: Alt+Tab vs. Tab; custom handler needs aria-label + NVSA testing), (3) regression matrix build (16+ tests for mixed formatting, alignment, indentation, copy/paste, undo/redo). Current session: Orchestration log recorded at .squad/orchestration-log/2026-04-21T21-27-24Z-neo.md. Session log: .squad/log/2026-04-21T21-27-24Z-latest-formatting-rejection.md. Team history updated. Ready for Neo to begin implementation work once product decision on Tab vs. Alt+Tab is confirmed and Trinity completes desktop font API revision (separate track).
